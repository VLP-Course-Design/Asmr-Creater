# ASMR Creater 部署文档

> 从零把仓库跑起来：环境 → 依赖 → 外部资源 → 启动 → 验证。
> 全部组件在本机运行，不依赖任何收费 API，也不上传用户图片。

配套文档：[README.md](README.md)（项目总览与目录结构）、[docs/json_contract.md](docs/json_contract.md)（契约说明）、[CONTRIBUTING.md](docs/CONTRIBUTING.md)（协作约定）。

---

## 1. 环境要求

| 项 | 要求 | 说明 |
|---|---|---|
| 操作系统 | Windows 10/11、macOS、Linux | 开发与验证在 Windows 11 上完成 |
| Python | 3.10 以上（实测 3.12.4） | 代码使用了 `X \| None` 形式的类型标注 |
| 内存 | ≥ 16 GB | 视觉语言模型 7B 量化推理约占 6~8 GB |
| 磁盘 | ≥ 12 GB 可用 | 模型 ~6 GB、素材库 ~2.1 GB、数据集 ~1.7 GB |
| 显卡 | 非必需 | 检测器固定 CPU 推理；模型走 Ollama，有显卡会更快 |
| 浏览器 | 支持 Web Audio API 的现代浏览器 | 音频执行全部在浏览器端完成 |

> **首次请求较慢**：检测器是懒加载的，第一次调用 `/infer` 需要加载权重，加上模型冷启动约 30 秒。演示或录屏前请先随便传一张图预热。

---

## 2. 安装依赖

### 2.1 建虚拟环境

```bash
python -m venv .venv
# Windows
.venv\Scripts\activate
# macOS / Linux
source .venv/bin/activate
```

### 2.2 安装运行时依赖

```bash
pip install -r requirements.txt
```

等价于：

```bash
pip install flask jsonschema ultralytics opencv-python numpy Pillow pyyaml openai
```

各包的用途：

| 包 | 用途 |
|---|---|
| `flask` | 后端服务与 `/infer` 等接口（`src/ui/app.py`） |
| `jsonschema` | 视觉记录 v2.3 与 Scene Contract v1.0 的契约校验 |
| `ultralytics` | YOLO11n 检测器，会自动带入 `torch` / `torchvision` |
| `opencv-python` | 图像读取、Letterbox 缩放 |
| `numpy` | 亮度计算与数组处理 |
| `Pillow` | EXIF 方向转正 |
| `pyyaml` | 读取 `configs/prompts.yaml` |
| `openai` | 通过 OpenAI 兼容协议访问本地 Ollama |

仅在**重新采集音频素材**时才需要，日常运行不装也可以（这三个包在 `sounds/fs_collect.py` 里是按需 import 的，已在 `requirements.txt` 中注释掉）：

```bash
pip install requests soundfile soxr
```

> `torch` / `torchvision` 由 `ultralytics` 自动带入，**不要单独声明**，以免与其锁定的版本冲突。

---

## 3. 准备外部资源

以下四类资源体积大，**均未纳入 Git 版本控制**（见 `.gitignore`），需要单独获取。

### 3.1 检测器权重（必需）

`yolo11n.pt` 约 5.5 MB，放在**仓库根目录**。若文件不存在，`ultralytics` 会在首次推理时自动下载；离线环境请预先从 Ultralytics 官方发布页下载并放好。

> 权重按相对路径解析，因此**所有命令都请在仓库根目录执行**。

### 3.2 Ollama 与视觉语言模型（必需）

1. 安装 [Ollama](https://ollama.com/) 并确认服务在 `http://localhost:11434` 监听。
2. 拉取模型（约 6 GB）：

```bash
ollama pull qwen2.5-vl:7b
ollama list          # 确认模型已就位
```

代码通过 OpenAI 兼容协议访问，端点固定为 `http://localhost:11434/v1`，`api_key` 传占位值 `ollama`（见 `src/vision/vibe_vlm.py`）。

> **模型不可用时系统不会崩溃**，而是回退到中性默认氛围继续完成推理。这一回退目前**不会在响应里显式标记**，前端也就无法区分「真实分析结果」和「回退结果」——演示前务必先访问 `/health` 确认服务状态。

### 3.3 音频素材库（必需）

素材本体（`sounds/**/*.wav`）不进 Git，仓库里只有目录结构、映射表、元数据与采集脚本。两种获取方式：

**方式一：解压素材交付包（推荐）**

素材线单独交付的压缩包解压到 `sounds/`，使目录形如：

```
sounds/
├── ambient/<sound_id>/<sound_id>_01.wav ...      # 20 类场景环境音
├── triggers/<sound_id>/<sound_id>_01.wav ...     # 66 类锚点关联声音
└── metadata.csv                                   # 215 条文件级元数据
```

**方式二：用采集脚本重新抓取**

需要先在 [Freesound](https://freesound.org/apiv2/apply/) 申请 API 凭据：

```bash
export FREESOUND_TOKEN=xxx           # 搜索必需
export FREESOUND_CLIENT_ID=xxx       # 下载原始 wav 需要 OAuth2
export FREESOUND_CLIENT_SECRET=xxx

python sounds/fs_collect.py search      # 只查询，产出 candidates.json，人工过一遍
python sounds/fs_collect.py auth        # OAuth2 授权
python sounds/fs_collect.py download    # 下载 + 转 48 kHz WAV + 写 metadata.csv
```

脚本把许可过滤写死为 CC0，母版统一转 48 kHz。采集口径、每类的目标时长与最少变体数见 `sounds/v23_registry.json`。

**校验素材是否齐全：**

```bash
python scripts/check_sounds_coverage.py
```

期望输出 `触发音 66/66，环境音 20/20`。

> ⚠️ **素材缺失不会报错，只会静默不响**。`configs/playback/audio_manifest.json` 是一份**已提交的静态清单**，播放计划只按 `asset_id` 引用它，不检查文件是否真的在磁盘上；文件缺失时计划照常生成，浏览器请求 WAV 得到 404，该音轨被跳过。所以务必用上面的覆盖率脚本确认，不要以「有声音」作为素材完整的判据。
>
> 另需注意：该清单目前**没有配套的生成脚本入库**，是按真实文件一次性生成后提交的。如果你增删了素材，需要手工同步这份 JSON（每条包含 `asset_id`、`sound_id`、`role`、`path`、`loop` 五个字段），否则新素材不会被任何播放计划引用。

### 3.4 图像数据集（可选）

`img_dataset/`（约 1.7 GB、2369 张）**仅离线批处理与复现 Prompt 实验时需要**，跑 Web 演示不需要。目录结构为 `img_dataset/Train/`（2000 张）与 `img_dataset/Val/`（369 张）。

---

## 4. 启动服务

```bash
python src/ui/app.py                 # 默认 0.0.0.0:5000
python src/ui/app.py --port 8080     # 指定端口
python src/ui/app.py --host 127.0.0.1
```

启动后终端会打印接口清单，浏览器访问 <http://localhost:5000> 即可。

后端暴露的接口：

| 接口 | 方法 | 说明 |
|---|---|---|
| `/` | GET | 前端单页应用 |
| `/health` | GET | 服务状态、当前模型标签、检测器是否已加载 |
| `/infer` | POST | 上传图像 → 视觉记录 v2.3 + 播放计划 + 素材 URL 表 |
| `/api/plan` | POST | 一条视觉记录 → 播放计划（供页面预设场景使用） |
| `/api/anchor_sound_map` | GET | 87 锚点 → 66 个受控 `sound_id` 的强度映射 |
| `/api/asset_urls` | GET | `asset_id` → 素材 URL 映射 |
| `/sounds/manifest` | GET | 现场扫描素材目录下的真实 WAV 清单 |
| `/sounds/<path>` | GET | 素材静态文件 |

> 这是 Flask 自带的开发服务器，**仅用于课程演示，不要直接暴露到公网**。如需长期运行，请用 `waitress` 或 `gunicorn` 承载。

---

## 5. 验证部署

### 5.1 自动化回归（不需要模型和素材）

四个脚本构成回归基线，**任何涉及契约、映射、配置或素材的改动都应先跑通它们**：

```bash
python -W ignore scripts/test_vision.py               # 期望：Results: 11 passed, 0 failed
python -W ignore scripts/test_playback_converter.py   # 期望：Ran 2 tests ... OK
python -W ignore scripts/validate_examples.py         # 期望：共 5 份, 全部通过契约校验
python -W ignore scripts/check_sounds_coverage.py     # 期望：触发音 66/66，环境音 20/20
```

`-W ignore` 用于抑制测试用例**故意构造**的容错分支所产生的 `UserWarning`（缺 `openai` 依赖、`global_vibe` 含未知字段等），它们不是错误。

### 5.2 播放计划端到端演示（不需要模型）

```powershell
powershell -ExecutionPolicy Bypass -File scripts\run_playback_demo.ps1
```

跑通后在 `outputs/playback/` 下得到两组结果：

- `with_depth/`：深度不确定度 0.18 < 阈值 0.35，门控通过，同时产出 `2.0-mono` 与 `2.0-binaural` 两份计划；
- `without_depth/`：以 `DEPTH_HINT_MISSING` 原因码退化，只产出 `2.0-mono`。

也可以直接调命令行转换器：

```bash
python src/audio/playback_converter.py \
  contracts/playback_proposal/examples/with_depth/example_coastal_bird_with_depth.json \
  --seed 18432 --duration 1800
```

### 5.3 全链路人工验证

1. `curl http://localhost:5000/health` → 返回 `status: ok` 与当前模型标签；
2. 浏览器打开首页，点任一预设场景 → 按播放键应听到环境床；
3. 上传一张自带图片 → 页面显示场景摘要与锚点列表，检测框贴合物体；
4. 观察后端日志，每次成功推理输出一行：场景类型、锚点数、计划版本、环境床 `sound_id`、前景层数。

---

## 6. 配置说明

### 6.1 环境变量

| 变量 | 默认值 | 说明 |
|---|---|---|
| `VLM_MODEL` | `qwen2.5-vl:7b` | 在 `src/ui/app.py` 中设定；`src/vision/vibe_vlm.py` 单独运行时默认为 `minicpm-v:8b` |
| `FREESOUND_TOKEN` | 无 | 仅素材采集需要 |
| `FREESOUND_CLIENT_ID` / `_SECRET` | 无 | 仅 OAuth2 下载原始 WAV 需要 |

### 6.2 音频决策配置（`configs/playback/`）

调整听感只需改配置，不必动源码与契约。

| 文件 | 作用 |
|---|---|
| `decision_settings.json` | 打分权重、候选阈值、层数上限、空间门控策略 |
| `scene_audio_profiles.json` | 场景 → 环境床映射（68 个叶子场景 + 20 个大组 + 静默兜底） |
| `audio_manifest.json` | 运行时素材清单（`asset_id` → 路径、角色、循环属性） |

需要留意的一项：`decision_settings.json` 中的 `allow_unverified_loops` 目前为 `true`。当前环境素材是程序批量采集的，无缝循环尚未经人耳逐条确认，清单里如实标记 `seamless_verified: false`。**若把这个开关改成 `false`，所有环境素材都会被拒绝，`/infer` 将返回 422。** 正确做法是先试听、把通过的素材逐条改成 `true`，再关闭开关。

### 6.3 Prompt

`configs/prompts.yaml` 是视觉层提示词（当前 v6），迭代记录见 `configs/PROMPT_CHANGELOG.md`。修改后建议先在小样本上验证再跑全量，避免重跑 2369 张的成本。

---

## 7. 离线批处理

仅在需要复现数据集实验时使用，均需先准备好 `img_dataset/`：

```bash
python scripts/run_vision_pipeline.py       # 预处理 + YOLO 检测演示
python scripts/normalize_global_vibe.py     # 旧 VLM 输出 → v2.3 形状
python scripts/export_downstream.py         # 导出给下游的 JSONL，并做契约校验
```

产物统一写到 `outputs/`（已在 `.gitignore` 中排除）。

---

## 8. 常见问题

**启动即报 `OSError: [WinError 1114] ... c10.dll`**
`torch` 动态库加载失败，通常是 Conda base 环境里 MKL/OpenMP 运行库冲突。建议在干净的 venv 里重装：`pip uninstall torch torchvision -y && pip install torch torchvision`。这只影响 YOLO 检测，播放计划转换器与素材脚本不依赖 torch，可以照常运行。

**终端里中文显示为乱码**
Windows 控制台默认 GBK。要么保持默认不做任何重定向，要么同时设置：`chcp 65001` 且 `set PYTHONIOENCODING=utf-8`。只设其中一个反而会乱码。

**页面有画面分析结果，但一点声音都没有**
按顺序排查：① `python scripts/check_sounds_coverage.py` 确认素材齐全；② 浏览器开发者工具 Network 面板看 `/sounds/...` 的 WAV 是否 404；③ 确认浏览器音频未被自动播放策略拦截（需先与页面交互）；④ 检查计划里 `layers.foreground` 是否为空——画面中没有命中锚点时本来就只有环境床。

**手机竖拍照片的检测框上下错位**
应已由 `ImageOps.exif_transpose` 修复。若仍出现，确认走的是 `src/vision/preprocess.py` 的统一入口，而不是直接用 `cv2.imread`（它会忽略 EXIF 方向）。

**`/infer` 返回 422**
契约校验或计划生成失败，响应体里带具体字段路径。常见原因：锚点缺 `bbox_norm`、枚举值越界、`allow_unverified_loops` 被关掉。

**端口被占用**
换端口：`python src/ui/app.py --port 8080`。

---

## 9. 交付清单

一份可运行的交付应包含：

- [ ] 代码仓库（本仓库，含 `configs/`、`contracts/`、`scripts/`）
- [ ] `yolo11n.pt` 放在仓库根目录
- [ ] `sounds/` 素材本体，覆盖率脚本输出 66/66 与 20/20
- [ ] 已安装 Ollama 且 `qwen2.5-vl:7b` 就位
- [ ] 四个回归脚本全部通过
- [ ] `img_dataset/`（仅在需要复现批处理实验时）
